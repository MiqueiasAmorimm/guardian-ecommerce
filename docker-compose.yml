version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: guardian-postgres
    restart: always
    environment:
      POSTGRES_DB: guardian_catalog
      POSTGRES_USER: guardian
      POSTGRES_PASSWORD: guardian
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  postgres-order:
    image: postgres:16-alpine
    container_name: guardian-postgres-order
    restart: always
    environment:
      POSTGRES_DB: guardian_order
      POSTGRES_USER: guardian
      POSTGRES_PASSWORD: guardian
    ports:
      - "5434:5432"
    volumes:
      - postgres_order_data:/var/lib/postgresql/data

volumes:
  postgres_data:
  postgres_order_data: